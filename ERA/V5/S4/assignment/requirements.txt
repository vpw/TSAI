# S4 cleaning pipeline. Install into .venv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
numpy<2            # fasttext's predict() breaks on numpy>=2 (np.array(copy=False))
pyarrow>=15
huggingface_hub>=0.25
regex>=2024.4
tokenizers>=0.19
fasttext-wheel>=0.9.2
