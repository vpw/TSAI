.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# logs/nbexec.log IS committed — dump_log.py exists precisely so the README's numbers can
# be checked without opening the notebook or rerunning it. (S11's .gitignore excluded it,
# which contradicted that tool's own docstring.)

# bootstrap-fetched at runtime by the notebook's own data cell — not authored content,
# re-downloaded fresh on each run. The generated plots ARE committed.
assets/tinyshakespeare.txt
